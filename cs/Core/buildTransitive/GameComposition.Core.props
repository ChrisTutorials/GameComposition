<Project>
  <PropertyGroup>
    <!-- Shared defaults for consuming projects. Keep minimal. -->
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
  </PropertyGroup>
</Project>
